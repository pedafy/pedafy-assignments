language: go

before_install:
  - openssl aes-256-cbc -K $encrypted_350a37d464b5_key -iv $encrypted_350a37d464b5_iv -in pedafy-251c5ecfe2ec.json.enc -out pedafy-251c5ecfe2ec.json -d

script:
  - go build -o server
  - go clean

# current version: 1
deploy:
  provider: gae
  keyfile: pedafy-251c5ecfe2ec.json
  project: pedafy
  # no_promote: false                   # If new version: true (def: false)
  # no_stop_previous_version: false     # If new version: true (def: false)
  on: master
